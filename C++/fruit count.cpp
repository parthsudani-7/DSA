#include<iostream>
using namespace std;

int count(int n, int arr[],int H){
    int min = 0;
    int max = 0;
    for(int i = 0; i < n; i++){
        if(arr[i] < min){
            min = arr[i];
        }
        if(arr[i] > max){
            max = arr[i];
        }
    }

    int mid = min + (max - min)/2;

    while(min < max ){
        for(int j = 0; j <)
    }
}


int  main(){
    count()
}