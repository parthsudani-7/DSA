Types of Tags:
1. Header
2. main
3. Nav
4. Artical
5. Section
6. Aside
7. Footer

semantic tags are HTML that clearly describe their meaning and purspose to both the browser and developers.

Inline: it takes only the space which is used by that perticular element.
Block: It takes whole line no dependency on how much is used,

1. <header> Tag
Purpose:
    Represents the top section of a webpage or a section.
Uses:
    Website logo
    Title or heading
    Navigation links
    Introductory content
Example:
    <header>
        <h1>My Website</h1>
        <p>Welcome to my site</p>
    </header>


2. <main> Tag
Purpose:
    Contains the main content of the webpage.
Uses:
    Holds the primary information
    Excludes header, footer, sidebar
    Only one <main> per page
Example:
    <main>
        <h2>Main Content</h2>
        <p>This is the core content of the page.</p>
    </main>

3. <nav> Tag
Purpose:
    Defines navigation links.
Uses:
    Menu bar
    Page links
    Internal or external navigation
Example:
    <nav>
        <a href="#">Home</a>
        <a href="#">About</a>
        <a href="#">Contact</a>
    </nav>

4. <article> Tag
Purpose:
    Represents independent, self-contained content.
Uses:
    Blog posts
    News articles
Forum posts
Example:
    <article>
        <h2>Blog Post</h2>
        <p>This is an article content.</p>
    </article>

5. <section> Tag
Purpose:
    Groups related content under a common theme.
Uses:
    Divide content into sections
    Organize topics
    Usually contains headings
Example:
    <section>
        <h2>Services</h2>
        <p>We offer web development services.</p>
    </section>

6. <aside> Tag
Purpose:
    Contains side content related to the main content.
Uses:
    Sidebar
    Ads
    Related links
    Author info
Example:
    <aside>
        <h3>Related Links</h3>
        <p>Check out our other articles.</p>
    </aside>

7. <footer> Tag
Purpose:
    Represents the bottom section of a page or section.
Uses:
    Copyright information
    Contact details
Social media links
Example:
    <footer>
        <p>© 2025 My Website</p>
    </footer>


1. Global Attributes
    Used with almost all HTML elements.
Common attributes:
    id → Unique identification
    class → Group elements
    style → Inline CSS
    title → Tooltip text
hidden → Hide element
Example:
    <p id="p1" class="text" title="Info">Hello</p>

2. Specific (Element-Based) Attributes
    Used with specific tags only.
Examples:
    href → <a>
    src → <img>
    alt → <img>
    type → <input>
Example:
    <a href="https://google.com">Google</a>
    <img src="img.jpg" alt="Image">
    <input type="text">


3. Boolean Attributes
    Do not need a value.
    Presence means true.
Examples:
    checked
    disabled
    required
Example:
    <input type="checkbox" checked>
    <button disabled>Submit</button>

4. Event Attributes
    Used to handle user actions.
Common events:
    onclick
    onmouseover
    onkeyup
Example:
    <button onclick="alert('Clicked')">Click</button>

5. Form Attributes
    Used in form elements.
Common attributes:
    name
    value
    placeholder
    min, max
Example:
    <input type="text" name="username" placeholder="Enter name">

6. Data Attributes
    Used to store custom data.
Rules:
    Must start with data-
    Used with JavaScript
Example:
    <button data-user="101">User</button>

7. ARIA Attributes
    Used for accessibility (screen readers).
Examples:           
    aria-label
    aria-hidden
Example:
    <button aria-label="Close">X</button>

SUMMARY:-
Global Attributes   → id, class, style, title
Specific Attributes → href, src, type
Boolean Attributes  → checked, disabled
Event Attributes    → onclick, onkeyup
Form Attributes     → name, placeholder
Data Attributes     → data-*
ARIA Attributes     → aria-label


📋 What is a List in HTML?
    An HTML list is a collection of items displayed in a specific order or format using list tags. Each item in a list is written inside a <li> (list item) tag.
🔹 Types of Lists in HTML

1️⃣ Ordered List (<ol>)
    Used when the order of items matters
    Items are numbered (1, 2, 3…), or can be letters/roman numerals

    <ol>
        <li>HTML</li>
        <li>CSS</li>
        <li>JavaScript</li>
    </ol>
Output:
    HTML
    CSS
    JavaScript
Common uses:
    Steps in a process
    Ranking or priority lists
    Instructions

2️⃣ Unordered List (<ul>)
    Used when the order does not matter
    Items are shown with bullets (•)
Syntax:
    <ul>
    <li>Apple</li>
    <li>Banana</li>
    <li>Mango</li>
    </ul>
Output:
    • Apple
    • Banana
    • Mango
Common uses:
    Navigation menus
    Feature lists
    General item collections

3️⃣ Description List (<dl>)
    Used for terms and their descriptions
    <dt> → term
    <dd> → description
Syntax:
    <dl>
    <dt>HTML</dt>
    <dd>HyperText Markup Language</dd>

    <dt>CSS</dt>
    <dd>Cascading Style Sheets</dd>
    </dl>
Common uses:
    Definitions
    FAQs
    Glossaries

✅ Key Points
    <li> is used inside <ol> and <ul>
    Lists can be nested (list inside another list)
    Lists improve readability and structure of webpages
    CSS can be used to style lists (remove bullets, change numbers, etc.)

🔹 Attributes Used in HTML Lists

1️⃣ Attributes of Ordered List (<ol>)
🔸Type Attribute
        Defines the numbering style of the ordered list.
    Values and meanings:
       1 → Numbers (default): 1, 2, 3
       A → Uppercase letters: A, B, C
       a → Lowercase letters: a, b, c
       I → Uppercase Roman numerals: I, II, III
       i → Lowercase Roman numerals: i, ii, iii
    Example:
        <ol type="A">
            <li>HTML</li>
            <li>CSS</li>
        </ol>

🔸Start Attribute
    Specifies the starting number of the ordered list.
    Example:
        <ol start="5">
            <li>Item One</li>
            <li>Item Two</li>
        </ol>
    Output: 5, 6

🔸 reversed Attribute
    Displays the ordered list in reverse order.
    Example:
        <ol reversed>
            <li>Step One</li>
            <li>Step Two</li>
        </ol>
    Output: 2, 1

2️⃣ Attributes of Unordered List (<ul>)
🔸 type Attribute (Deprecated in HTML5 but still asked in exams)
    Defines the bullet style of the list.
    Values and meanings:
        disc → ● Default
        circle → ○
        square → ■
    Example:
        <ul type="square">
            <li>Pen</li>
            <li>Pencil</li>
        </ul>
Note: This attribute is deprecated in HTML5. CSS should be used instead.

3️⃣ Attributes of List Item (<li>)
🔸 value Attribute (Used only with ordered lists)
    Changes the number of a specific list item.
    Example:
        <ol>
            <li>One</li>
            <li value="5">Five</li>
            <li>Six</li>
        </ol>
    Output: 1, 5, 6

4️⃣ Description List (<dl>) Attributes
    ❌ <dl>, <dt>, and <dd> do not have specific list-related attributes
    ✔ Only global attributes can be applied

5️⃣ Global Attributes (Applicable to All List Tags)
    These attributes can be used with <ol>, <ul>, <li>, <dl>, <dt>, and <dd>:
        id
        class
        style
        title
    Example:
        <ul class="menu" id="nav">
            <li>Home</li>
        </ul>

In HTML, a table is used to display data in rows and columns, just like a spreadsheet. Tables are mainly used for tabular data such as marks, prices, schedules, records, etc.
📊 What is a Table in HTML?
    An HTML table arranges data into rows (<tr>) and columns (<td> / <th>) inside the <table> tag.
    Each row can contain one or more cells.
🧩 Basic Table Structure
        <table>
        <tr>
            <th>Heading 1</th>
            <th>Heading 2</th>
        </tr>
        <tr>
            <td>Data 1</td>
            <td>Data 2</td>
        </tr>
        </table>

🏷️ Common Table Tags
        <table>
         Defines the table

        <tr> (Table Row)
         Defines a row in the table

        <th> (Table Header)
         Defines a header cell (bold and centered by default)

        <td> (Table Data)
         Defines a normal data cell

        <caption>
         Adds a title to the table
        
📌 Simple Example
<table border="1">
  <caption>Student Marks</caption>
  <tr>
    <th>Name</th>
    <th>Marks</th>
  </tr>
  <tr>
    <td>Parth</td>
    <td>85</td>
  </tr>
  <tr>
    <td>Rahul</td>
    <td>90</td>
  </tr>
</table>

✅ Key Points
Tables are used only for data, not for page layout
Rows are created using <tr>
Columns are created using <td> or <th>
<th> is used for headings
Tables can be styled using CSS
🧠 Real-World Uses
    Exam results
    Product price lists
    Timetables
    Reports and records